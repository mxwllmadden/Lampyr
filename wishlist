

Ok so lampyr rig calibrate works great

lampyr run X works great too, even allows mouse updating
but we need more telemetry regarding previous trials

make lampyr run work through Lampyr?
Is Lampyr the app or the basic Behavior

I think Lampyr should be the app. So in that case we bundle lots of system
info and stuff into lampyr. All run commands should be as simple as calls to Lampyr

Therefore Lampyr should handle the saving.

Lampyr loads the mouse info if there is a mouse flag
Lampyr allows saving but also is reponsible for saving and updating the mouse file at the end.
Each parser should just be a lampyr dot namespace

Lampyr.run(X with mouse)
Lampyr.mouse to make mice


6/6/2025
WE CAN NOW RUN HABITUATION
todo:
    determine order of operations for lampyr object
        config
        mouse
        rig / mouse / session data
        behavior
    enable mouse command, should be mouse [mouseid] [command] -flags
        lampyr mouse run [mouseid]
        lampyr mouse create [mouseid] -params
        lampyr mouse run [mouseid] -behavior
    create the paradigm object
    rewrite bandit in new format

TODO
ADDITIONAL MOUSE Data Object
 - Metadata from each behavior that was run on the mouse including:
     - Properties
     - Reports
     - Merits, demerits, rewards, etc
     - Name of the datafile for access to the full file
     - Allows decisionmaking based on the mouse info
BEHAVIOR Object
 - Create systematic generation of names. Should be:
 - Date-time_Mouse_HierarchicalNamewithnumbers.behav.lmp
 - Pass reference to parent to generate the name.
DATA STORAGE
 - Collect data and put it into h5 format similar to behavior. Should be 1 file per day as backup
 - Each datafile should be 1 session
 - Values
 - rig
 - subdata etc.
BANDIT
 - Go through and really think about progression and stop condition logic
LAMPYR
 - Move all handles and functionality over to lampyr
 - Lampyr organizes all data and composes the app functionality
 - Lampyr manages, opens, and closes the rigs, data, etc as well as the behavior specified